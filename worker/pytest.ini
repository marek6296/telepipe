[pytest]
pythonpath = src
testpaths = tests
asyncio_mode = auto
markers =
    integration: proti reálnej DB (spúšťa sa ručne: pytest -m integration)
addopts = -m "not integration"
# test_role.py (SERVICE_ROLE/fanvue split + railway.json template layoutu) čaká na fázu 3.
norecursedirs = _pending_t12
